import { fmfConstant } from '../../../helpers/fmf-constant';
import { module, test } from 'qunit';

module('Unit | Helper | fmf constant');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fmfConstant(42);
  assert.ok(result);
});
