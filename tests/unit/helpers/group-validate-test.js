import { groupValidate } from '../../../helpers/group-validate';
import { module, test } from 'qunit';

module('Unit | Helper | group validate');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = groupValidate(42);
  assert.ok(result);
});
